# ACCA FA 学習アプリ

> **このリポジトリでセッションを始めたら、まず `docs/学習_引き継ぎ.md` を読む。**
> どこまで進んだか・用語の統一・次にやる範囲がそこにある。

---

## 1. これは何か

ACCA **FA（Financial Accounting）** を独学するための単一HTMLアプリ。

| | |
|---|---|
| 正本 | `C:\Dev\acca-study`（GitHub: `yujirai0926OK/acca-study`） |
| 公開 | https://yujirai0926ok.github.io/acca-study/ |
| 機能 | 要点／単語帳（Leitner）／確認テスト／英語読み上げ（Web Speech API）／📕本棚（現在は主役から外した） |
| 同期 | **Firebase 有効**（`FB_CONFIG` 設定済・projectId `acca-study`）。進捗・単語・自作テスト・設定のみ |

**このセッションの主な仕事は「ユーザーが読んで詰まった箇所を解説し、アプリに貯めること」。**

---

## 2. 著作権（最優先。ここだけは破らない）

Kaplan の教科書は他社の著作物。**私的利用の範囲を出さない設計にしてある。**

- **教科書の本文をこのリポジトリにコミットしない・push しない**
- アプリに入る `SECTIONS` / `SEED_WORDS` / `QUIZ` は**こちらの書き下ろし**。**ここに原文を貼らない**
- 例文は教科書の文を写さず、その語の使い方が分かる短い文を新しく書く
- 写真やPDFでページを見せられても、**読んで解説するだけ**。本文を書き写して保存しない
- `translations/` は gitignore 済み。GitHub Pages で公開されるのは**アプリのコードだけ**

教材PDFの場所：`C:\Users\tadam\OneDrive\Desktop\ACCA\FAtext\`（p1〜40のみ・BibliUの保存上限で残りは無い）
**リポジトリにコピーしない。**

---

## 3. 運用（2026-08-22 に変更）

### 経緯

教科書（BibliU）は**コピーできない**。以前の「教科書の英文を対訳にして本棚に読み込む」運用は、
ユーザーが英文を渡せる前提だったため成立しない。
加えて**本棚は端末内のみで同期されない**ため、iPhoneでの復習にも使えなかった。

### いまの流れ

```
読む → 詰まる → 写真か入力でClaudeに渡す → 解説 → アプリに貯める
```

| 中身 | 入れる場所 | 同期 |
|---|---|---|
| 語彙（訳・解説・例文） | `SEED_WORDS` | ○ |
| 構文・語法・論点の解説 | `SECTIONS` | ○ |
| 確認テスト | `QUIZ` | ○ |

**すべて `index.html` に入る。** push すれば全端末で読める。ユーザー側の貼り付け作業は不要
（単語だけ、アプリの「最新の収録単語を取り込む」を1回押す）。

### 書き方

- `SEED_WORDS` は `["語","訳","解説","例文"]` の4要素。解説は1〜2文、例文は短く
- `SECTIONS` は `{n, ja, en, pts_ja[], pts_en[]}`。語彙・構文まわりの `n` は `英1` `英2` … と付け、章の要点と混ぜない
- `QUIZ` は `{q, opts[], a, ex}`。`a` は正解のインデックス（0始まり）
- 読み手は管理部門ひとすじ30年の経理責任者。**会計の中身は既知で、英語だけが壁**
- 会計用語は**日本の実務語**に寄せる。訳語は `docs/学習_引き継ぎ.md` の一覧に従い、新しく決めたらそこに足す
- 日本基準と違う論点は**1行だけ**注記する。長い解説は書かない

### 単語のたまり方

覚えたい語はその都度 push せず、`SEED_WORDS` 末尾に追記して未コミットで溜める。
**まとまった時点でユーザーの合図で一括コミット＆push。**

### 本棚（マイ対訳）

**主役から外した。** 機能は残すが、新しく対訳JSONを作らない。

---

## 4. 学習の文脈（分量を決めるのに要る）

- FA本編は **p21〜p448 の428ページ／全20章**（第21章＝Practice Questions、第22章＝Practice Answers）
- 週の持ち分は **約25ページ**。平日朝＝読む、土日＝Exam Kitを解く
### 読むことと覚えることを分ける（2026-08-22決定）

> ⚠ 以前ここには「**英語を先に読む。解説は答え合わせに使う。先に日本語を読むと英語が身につかない**」と書いてあった。
> **答え合わせという位置づけは撤回した。** W1の実測が28分/ページで、428ページ＝約200時間。
> 器（20週×9時間＝180時間）を読むだけで超えるため、1周目の速度を犠牲にする設計は取れない。
> 理由と却下案は保管庫の `判断ログ\20260822_ACCA学習法_読むと覚えるを分ける.md`。

| | やり方 | このセッションの役割 |
|---|---|---|
| **読むとき** | 英文を読み、わからない単語・文はすぐ渡す。**理解を優先して速度を落とさない** | **主戦場。** 遠慮なく日本語で説明してよい |
| **覚えるとき** | アプリで単語と解説を回す。**単語力と文章力はここでつける** | `SEED_WORDS` / `SECTIONS` / `QUIZ` に貯める |
| **問題集** | **まず自力で解く** | **先回りして手を出さない** |

**英語は捨てていない。読んでいる最中ではなく、後工程で身につける。** だから解説は出し惜しみしない。

### 問題集（Exam Kit）には、解く前に触らない

**本番は2時間・1点あたり1.2分・辞書もAIもなし。問題集が唯一の実戦の場。**

- **解く前に、問題文を訳さない・解説しない・ヒントを出さない**
- 頼まれたら、**解き終わったあとかどうかを先に確認する**
- 出番は**解いたあと**。間違えた問題と、どうしてもわからなかった問題だけ
- そこで出た語彙・論点は、テキストのときと同じように `SEED_WORDS` / `SECTIONS` に貯める

### 分量

- **章まるごとを先回りしてやらない。** その週に読む範囲か、詰まった箇所だけ
  → 理由は「英語を守るため」ではなく、**復習の単位を、読んだ範囲に揃えるため**
- 全体計画の正本：保管庫の `判断ログ\20260817_ACCA学習スケジュールとnote連載の両立.md`
- 制度・期限の一次情報：保管庫の `調べもの\20260817_ACCA2027年改定とApplied Knowledgeの期限.md`

---

## 5. コードを触るとき

- **`FB_CONFIG` を書き換えない**（Firebaseの接続設定。稼働中）
- push 前に必ず実行する：
  ```bash
  node runcheck.js index.html
  node mergetest.js
  ```
  構文チェックだけでは、**変数の宣言順ミスでアプリ全体が無反応になる不具合**を検出できなかった実績がある
- 同期の設計判断は保管庫の `判断ログ\20260811_ACCAアプリ_Firebase同期方針.md` と `20260812_ACCA学習アプリのクラウド同期方針.md`

---

## 6. note連載との関係

この学習は note連載「52歳、ACCAを学び直す」（毎週月曜9:00）の題材でもある。
連載側の正本は **別リポジトリ** `C:\Dev\acca-note`。**このセッションでは記事を書かない。**
読んでいて「日本の実務とここが違う」と気づいたことがあれば、
`docs/学習_引き継ぎ.md` の「記事の種」に1行だけ残す。
