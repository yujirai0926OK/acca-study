# ACCA FA 学習アプリ ／ 教科書の対訳づくり

> **このリポジトリでセッションを始めたら、まず `docs/翻訳_引き継ぎ.md` を読む。**
> どこまで訳したか・用語の統一・次にやる範囲がそこにある。

---

## 1. これは何か

ACCA **FA（Financial Accounting）** を独学するための単一HTMLアプリ。

| | |
|---|---|
| 正本 | `C:\Dev\acca-study`（GitHub: `yujirai0926OK/acca-study`） |
| 公開 | https://yujirai0926ok.github.io/acca-study/ |
| 機能 | 📖 英日対訳リーダー（**📕本棚**）／単語帳（Leitner）／確認テスト／英語読み上げ（Web Speech API） |
| 同期 | **Firebase 有効**（`FB_CONFIG` 設定済・projectId `acca-study`）。進捗・単語・自作テスト・設定のみ |

**このセッションの主な仕事は「教科書の英文を対訳にして、本棚に読み込めるJSONにする」こと。**

---

## 2. 著作権（最優先。ここだけは破らない）

Kaplan の教科書は他社の著作物。**私的利用の範囲を出さない設計にしてある。**

- **教科書の本文をこのリポジトリにコミットしない・push しない**
- 対訳は **本棚＝端末の localStorage だけ**。**Firebaseにも同期しない**（アプリ側でそう作ってある）
- 生成した対訳JSONは `translations/` に置く。**gitignore 済み**
- GitHub Pages で公開されるのは**アプリのコードだけ**。教材本文は絶対に載せない
- アプリに最初から入っている `SECTIONS` / `SEED_WORDS` は**私の要約と用語集**であって本文の複製ではない。ここに原文を貼らない

教材PDFの場所：`C:\Users\tadam\OneDrive\Desktop\ACCA\FAtext\`（p1〜40のみ・BibliUの保存上限で残りは無い）
**リポジトリにコピーしない。**

---

## 3. 対訳の作り方（このセッションの成果物）

**入力**：ユーザーが渡す英文（写真・PDF・貼り付け）
**出力**：本棚に読み込める JSON ファイル1つ

### 形式

```json
{
  "type": "acca-bookshelf",
  "passages": [
    {
      "id": "c01-s03",
      "title": "第1章-3 財務諸表の主要素",
      "en": "The elements of financial statements are ...",
      "ja": "財務諸表の構成要素は……"
    }
  ]
}
```

- **`id` か `title` が既存と一致すれば上書き、違えば追加**される（アプリの読み込み処理がそう動く）
  → 訳し直すときは **同じ id を使う**。追加したいときは新しい id
- ユーザー側の取り込み手順：アプリ →「📕 本棚だけを移す」→ **⬆️ 本棚を読み込み（追加）**
- **進捗・単語・自作テストには一切影響しない。** 安心して何度でも読み込める

### `title` の付け方

`第N章-節番号 見出し` （例：`第1章-3 財務諸表の主要素`）

**N は「本の章番号」。** MyKaplanの学習プログラムのセクション番号は本の章番号より1つ大きいので、そちらを使わない。

### `en` の扱い

- **原文の改行をそのまま保つ。** 読み上げ機能がこの文字列を読むため
- 表・数値例は無理に入れない。文章だけを対象にする
- 1件が長すぎると本棚で開いたとき読みにくい。**節または段落のまとまりで切る**

### 訳しかた

- **逐語訳にしない。** 読み手は管理部門ひとすじ30年の経理責任者。会計の中身は既知で、英語だけが壁
- 会計用語は**日本の実務語**に寄せる（例：statement of financial position → 財政状態計算書）
- **用語の対訳は `docs/翻訳_引き継ぎ.md` の一覧に従う。** 新しく決めたらそこに足す
- 日本基準と違う論点（再評価モデル、IFRS 18の表示区分など）は、訳文の後に **1行だけ**注記してよい。長い解説は書かない

---

## 4. 学習の文脈（訳す量を決めるのに要る）

- FA本編は **p21〜p448 の428ページ／全20章**（第21章＝Practice Questions、第22章＝Practice Answers）
- 週の持ち分は **約25ページ**。平日朝＝読む、土日＝Exam Kitを解く
- **英語を先に読む。対訳は答え合わせに使う。** 先に日本語を読むと英語が身につかない
  → だから**章まるごとを先回りして訳さない。** その週に読む範囲、または詰まった箇所を訳す
- 全体計画の正本：保管庫の `判断ログ\20260817_ACCA学習スケジュールとnote連載の両立.md`
- 制度・期限の一次情報：保管庫の `調べもの\20260817_ACCA2027年改定とApplied Knowledgeの期限.md`

---

## 5. コードを触るとき

- **`FB_CONFIG` を書き換えない**（Firebaseの接続設定。稼働中）
- push 前に必ず実行する：
  ```bash
  node runcheck.js index.html
  node mergetest.js
  ```
  構文チェックだけでは、**変数の宣言順ミスでアプリ全体が無反応になる不具合**を検出できなかった実績がある
- 同期の設計判断は保管庫の `判断ログ\20260811_ACCAアプリ_Firebase同期方針.md` と `20260812_ACCA学習アプリのクラウド同期方針.md`

---

## 6. note連載との関係

この学習は note連載「52歳、ACCAを学び直す」（毎週月曜9:00）の題材でもある。
連載側の正本は **別リポジトリ** `C:\Dev\acca-note`。**このセッションでは記事を書かない。**
訳していて「日本の実務とここが違う」と気づいたことがあれば、
`docs/翻訳_引き継ぎ.md` の「記事の種」に1行だけ残す。
